# Copy this file to terraform.tfvars and uncomment any overrides you want.
# terraform.tfvars is gitignored; the example is committed for reference.
#
# All variables have defaults; an empty terraform.tfvars (or none at all) is valid.

# aws_region        = "us-east-1"
# availability_zone = "us-east-1a"
# instance_type     = "t4g.small"
# root_volume_gb    = 20
# app_user          = "filings"
